package types

// ChannelId identifies a messaging channel (e.g. "telegram", "discord", "whatsapp").
type ChannelId string

// SessionKey is the composite key for an agent session (e.g. "tg:123:main").
type SessionKey string
